name: windows

on:
  workflow_dispatch:
    inputs:
      os:
        default: "windows-2019"
        required: false

jobs:
  windows:
    runs-on: ${{ inputs.os }}
    steps:
      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
